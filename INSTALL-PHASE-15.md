# Install Phase 15 — Student Experience Bridge

This is a Phase 14-compatible patch. Extract it and upload the included files
to their matching paths in the GitHub repository, then commit to `main`.

Keep the proven Blogger setting `ENABLE_V2: true` if the Phase 13 runtime test
is still normal. No theme change is required. GitHub Actions will publish the
updated `dist/v2/` runtime after the commit.
