# Sykabelajar Frontend v4.3 Control Plane

This build keeps Blogger as application shell and loads fingerprinted assets through the stable `dist/loader.js` URL.

### Control plane routes
- `/admin`
- `/organizer`

The admin and organizer pages are tabbed control planes, not placeholder dashboards.
